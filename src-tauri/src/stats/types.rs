//! Stats data types — re-exported from the shared type registry.

#[expect(unused_imports)]
pub use crate::shared::types::{
    ActivityPoint, DayNightSplit, HeatmapPoint, StatsData, StreaksData, TopAlbum, TopArtist,
    TopGenre, TopTrack, TrendsData, WeekDayStatus, WeeklyWrapData,
};
